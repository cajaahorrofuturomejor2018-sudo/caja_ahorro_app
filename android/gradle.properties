org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
org.gradle.java.installations.auto-download=true

# If you want to force Gradle to use a specific local JDK (for example JDK 21),
# install JDK 21 on your machine and then set the path below (uncomment and edit).
# Example Windows path:
# org.gradle.java.home=C:\\Program Files\\Java\\jdk-21
#
# Notes:
# - Gradle will use the JDK on the PATH by default. To ensure builds use Java 21,
#   either set `org.gradle.java.home` above or set the system JAVA_HOME to your
#   JDK 21 installation and restart the terminal/IDE.
# - After installing JDK 21, run `./gradlew.bat -v` (from the android folder) to
#   confirm which Java runtime Gradle is using.

