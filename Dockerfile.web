# Stage 1 (optional): expects you already ran `flutter build web`
# This Dockerfile packages the built Flutter Web app using Nginx.
# Build image (after building web):
#   flutter build web --release
#   docker build -f Dockerfile.web -t caja_ahorro_web:latest .
# Run:
#   docker run -p 8080:80 caja_ahorro_web:latest

FROM nginx:alpine

# Remove default config and add our own (optional)
COPY docker/web/nginx.conf /etc/nginx/conf.d/default.conf

# Copy Flutter web build output
COPY build/web /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
