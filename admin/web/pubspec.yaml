name: admin_web
description: Admin web frontend using Flutter Web
publish_to: none
version: 0.0.1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.12.0
  firebase_auth: ^4.6.0
  http: ^0.13.6
  flutter_dotenv: ^5.0.2

flutter:
  uses-material-design: true
